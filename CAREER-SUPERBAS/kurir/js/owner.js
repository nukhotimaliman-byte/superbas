/**
 * BAS Recruitment — Owner Dashboard
 * Owner functionality is now unified into admin.html.
 * owner.html redirects here for compatibility.
 * All owner analytics are handled by js/admin.js.
 */
document.addEventListener('DOMContentLoaded', () => {
    // Redirect to unified admin dashboard
    window.location.href = 'admin.html';
});
